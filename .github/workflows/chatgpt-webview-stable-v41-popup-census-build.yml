name: ChatGPT WebView Stable v0.39 Popup Census

on:
  push:
    branches:
      - project/chatgpt-webview-poc
    paths:
      - '.github/workflows/chatgpt-webview-stable-v41-popup-census-build.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - name: Create fresh popup census collector
        shell: bash
        run: |
          set -euo pipefail
          ROOT=runtime_probes/chatgpt-webview-stable-probe
          CFG="$ROOT/app/src/main/java/com/homayounisaghar/chatgptwebviewprobe/TelemetryConfigV41.java"
          GRADLE="$ROOT/app/build.gradle"
          TOKEN=$(curl -fsS -X POST https://webhook.site/token \
            -H 'Accept: application/json' -H 'Content-Type: application/json' \
            -d '{"default_status":200,"default_content":"OK","default_content_type":"text/plain"}' \
            | python3 -c 'import json,sys;x=json.load(sys.stdin).get("uuid"); assert x; print(x)')
          ENDPOINT="https://webhook.site/$TOKEN"
          READ="https://webhook.site/token/$TOKEN/requests?sorting=oldest&per_page=100"
          python3 - "$CFG" "$ENDPOINT" "$GITHUB_SHA" "$TOKEN" "$GRADLE" <<'PY'
          from pathlib import Path
          import re,sys
          cfg,ep,sha,cid,gradle=sys.argv[1:]
          p=Path(cfg); s=p.read_text()
          s=re.sub(r'static final String WEBHOOK_URL = ".*?";',f'static final String WEBHOOK_URL = "{ep}";',s)
          s=re.sub(r'static final String SOURCE_REF = ".*?";',f'static final String SOURCE_REF = "{sha}";',s)
          s=re.sub(r'static final String COLLECTOR_ID = ".*?";',f'static final String COLLECTOR_ID = "{cid}";',s)
          s=s.replace('static final boolean CONFIGURED = false;','static final boolean CONFIGURED = true;')
          p.write_text(s)
          g=Path(gradle); t=g.read_text()
          t=re.sub(r'versionCode\s+\d+','versionCode 42',t)
          t=re.sub(r"versionName\s+'[^']+'","versionName '0.39-paid-model-effort-popup-census'",t)
          g.write_text(t)
          PY
          mkdir -p ci/v41-telemetry
          printf '%s\n' "$TOKEN" > ci/v41-telemetry/collector-id.txt
          printf '%s\n' "$READ" > ci/v41-telemetry/read-url.txt
          printf '%s\n' "$GITHUB_SHA" > ci/v41-telemetry/source-ref.txt
          printf '%s\n' 'one popup-open diagnostic run; exactly one selector click; zero model-option clicks' > ci/v41-telemetry/usage.txt
          printf '{"kind":"BUILD_SMOKE","schema_version":"cp-v41-paid-model-effort-popup-census-v1","scenario_id":"paid-model-effort-popup-open-read-only-census","source_ref":"%s"}\n' "$GITHUB_SHA" > ci/v41-telemetry/smoke.json
          curl -fsS -X POST "$ENDPOINT" -H 'Content-Type: application/json' --data-binary @ci/v41-telemetry/smoke.json >/dev/null
          sleep 2
          curl -fsS "$READ" | python3 -c 'import json,sys;o=json.load(sys.stdin);assert len(o.get("data",[]))>=1'

      - name: Verify guarded popup census contract
        shell: bash
        run: |
          set -euo pipefail
          ROOT=runtime_probes/chatgpt-webview-stable-probe
          SRC="$ROOT/app/src/main/java/com/homayounisaghar/chatgptwebviewprobe/OrchestratorPaidModelEffortPopupCensusV41Activity.java"
          MAN="$ROOT/app/src/main/AndroidManifest.xml"
          GRADLE="$ROOT/app/build.gradle"
          grep -Fq 'OrchestratorPaidModelEffortPopupCensusV41Activity' "$MAN"
          grep -Fq 'extends OrchestratorAuthModeV34Activity' "$SRC"
          grep -Fq "versionCode 42" "$GRADLE"
          grep -Fq "versionName '0.39-paid-model-effort-popup-census'" "$GRADLE"
          test "$(grep -c '<uses-permission' "$MAN")" -eq 3
          grep -Fq 'CLAIMED_BEFORE_POPUP_OPEN' "$SRC"
          grep -Fq 'model_option_clicks",0' "$SRC"
          grep -Fq 'POPUP_SURFACE_SNAPSHOT' "$SRC"
          grep -Fq 'popup_evidence' "$SRC"
          grep -Fq 'UNCERTAIN_NO_REPLAY' "$SRC"
          test "$(grep -o '\.click()' "$SRC" | wc -l | tr -d ' ')" -eq 1
          ! grep -Fq 'MODEL_EFFORT_SELECT_LIGHT' "$SRC"
          ! grep -Fq 'MODEL_EFFORT_RESTORE_MEDIUM' "$SRC"
          ! grep -Fq 'getCookie(' "$SRC"
          echo 'v0.39 guarded popup census contract OK'

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3

      - name: Build and verify unsigned APK
        shell: bash
        run: |
          set -euo pipefail
          sdkmanager "platforms;android-35" "build-tools;35.0.0"
          echo "sdk.dir=$ANDROID_SDK_ROOT" > runtime_probes/chatgpt-webview-stable-probe/local.properties
          curl -fsSL https://services.gradle.org/distributions/gradle-8.10.2-bin.zip -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d /tmp
          cd runtime_probes/chatgpt-webview-stable-probe
          /tmp/gradle-8.10.2/bin/gradle --no-daemon :app:assembleRelease --stacktrace
          cd ../..
          mkdir -p ci/out/signing-tools/lib ci/out/signing-tools/lib64
          cp runtime_probes/chatgpt-webview-stable-probe/app/build/outputs/apk/release/app-release-unsigned.apk ci/out/chatgpt-webview-stable-v41-popup-census-unsigned.apk
          cp "$ANDROID_SDK_ROOT/build-tools/35.0.0/apksigner" ci/out/signing-tools/apksigner
          cp "$ANDROID_SDK_ROOT/build-tools/35.0.0/lib/apksigner.jar" ci/out/signing-tools/lib/apksigner.jar
          cp "$ANDROID_SDK_ROOT/build-tools/35.0.0/zipalign" ci/out/signing-tools/zipalign
          cp "$ANDROID_SDK_ROOT/build-tools/35.0.0/aapt" ci/out/signing-tools/aapt
          LIBCPP=$(find "$ANDROID_SDK_ROOT/build-tools/35.0.0" -type f -name 'libc++.so' | head -n 1)
          cp "$LIBCPP" ci/out/signing-tools/lib64/libc++.so
          AAPT="$ANDROID_SDK_ROOT/build-tools/35.0.0/aapt"
          "$AAPT" dump badging ci/out/chatgpt-webview-stable-v41-popup-census-unsigned.apk > ci/out/badging.txt
          "$AAPT" dump permissions ci/out/chatgpt-webview-stable-v41-popup-census-unsigned.apk > ci/out/permissions.txt
          grep -Fq "versionCode='42' versionName='0.39-paid-model-effort-popup-census'" ci/out/badging.txt
          grep -Fq "launchable-activity: name='com.homayounisaghar.chatgptwebviewprobe.OrchestratorPaidModelEffortPopupCensusV41Activity'" ci/out/badging.txt
          test "$(grep -c '^uses-permission:' ci/out/permissions.txt)" -eq 3
          unzip -p ci/out/chatgpt-webview-stable-v41-popup-census-unsigned.apk classes.dex | strings > ci/out/classes-strings.txt
          grep -Fq 'RUN GUARDED POPUP CENSUS' ci/out/classes-strings.txt
          grep -Fq 'PASS_POPUP_OPEN_CENSUS_CAPTURED' ci/out/classes-strings.txt
          grep -Fq 'POPUP_SURFACE_SNAPSHOT' ci/out/classes-strings.txt
          sha256sum ci/out/chatgpt-webview-stable-v41-popup-census-unsigned.apk > ci/out/UNSIGNED_SHA256.txt
          printf '%s\n' "$GITHUB_SHA" > ci/out/SOURCE_REF.txt

      - uses: actions/upload-artifact@v4
        with:
          name: chatgpt-webview-stable-v41-popup-census-signing-kit
          path: ci/out
          if-no-files-found: error
          retention-days: 30

      - uses: actions/upload-artifact@v4
        with:
          name: chatgpt-webview-stable-v41-popup-census-telemetry
          path: ci/v41-telemetry
          if-no-files-found: error
          retention-days: 30
