name: ChatGPT WebView Stable v0.83 Fingerprint Drift Probe Unsigned

on:
  push:
    branches:
      - project/chatgpt-webview-poc-v083-fingerprint-drift-probe-staging9
      - project/chatgpt-webview-poc
    paths:
      - '.github/workflows/chatgpt-webview-stable-v85-resource-fingerprint-drift-probe-unsigned.yml'
      - 'ci/generate_chatgpt_webview_v85_resource_fingerprint_drift_probe.py'
      - 'ci/generate_chatgpt_webview_v84_resource_semantic_turn_identity.py'
      - 'ci/generate_chatgpt_webview_v83_resource_observable_terminal.py'
      - 'ci/generate_chatgpt_webview_v82_calibrated_actuator_resource_fix1.py'
      - 'ci/generate_chatgpt_webview_v82_calibrated_actuator_resource.py'
      - 'ci/generate_chatgpt_webview_v81_assistant_local_resource_acceptance.py'
      - 'ci/generate_chatgpt_webview_v80_assistant_local_interaction_probe.py'
      - 'ci/generate_chatgpt_webview_v79_resource_topology_probe.py'
      - 'ci/generate_chatgpt_webview_v78_resource_final_bounded_wrapped.py'
      - 'ci/generate_chatgpt_webview_v77_resource_provenance_hardened.py'
      - 'ci/generate_chatgpt_webview_v77_resource_provenance_hardened_fix1.py'
      - 'ci/generate_chatgpt_webview_v76_resource_tools_consolidated.py'
      - 'ci/generate_chatgpt_webview_v76_resource_tools_consolidated_fix1.py'
      - 'ci/generate_chatgpt_webview_v64_conversation_runtime.py'
      - 'ci/v51_model_observer/ControlPlaneAccessibilityServiceV51.java'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Verify checkout identity
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"

      - name: Generate v0.83 fingerprint drift diagnostic source
        run: python3 ci/generate_chatgpt_webview_v85_resource_fingerprint_drift_probe.py

      - name: Create fresh telemetry collector
        shell: bash
        run: |
          set -euo pipefail
          CFG=runtime_probes/chatgpt-webview-stable-probe/app/src/main/java/com/homayounisaghar/chatgptwebviewprobe/TelemetryConfigV85.java
          TOKEN=$(curl -fsS -X POST https://webhook.site/token -H 'Accept: application/json' -H 'Content-Type: application/json' -d '{"default_status":200,"default_content":"OK","default_content_type":"text/plain"}' | python3 -c 'import json,sys;x=json.load(sys.stdin).get("uuid");assert x;print(x)')
          ENDPOINT="https://webhook.site/$TOKEN"
          READ="https://webhook.site/token/$TOKEN/requests?sorting=oldest&per_page=100"
          python3 - "$CFG" "$ENDPOINT" "$GITHUB_SHA" "$TOKEN" <<'PY'
          from pathlib import Path
          import re,sys
          p=Path(sys.argv[1]); s=p.read_text(); ep,sha,cid=sys.argv[2:]
          s=re.sub(r'WEBHOOK_URL=".*?"',f'WEBHOOK_URL="{ep}"',s)
          s=re.sub(r'SOURCE_REF=".*?"',f'SOURCE_REF="{sha}"',s)
          s=re.sub(r'COLLECTOR_ID=".*?"',f'COLLECTOR_ID="{cid}"',s)
          s=s.replace('CONFIGURED=false','CONFIGURED=true')
          p.write_text(s)
          PY
          mkdir -p ci/v85-resource-fingerprint-drift-probe-telemetry
          printf '%s\n' "$TOKEN" > ci/v85-resource-fingerprint-drift-probe-telemetry/collector-id.txt
          printf '%s\n' "$READ" > ci/v85-resource-fingerprint-drift-probe-telemetry/read-url.txt
          printf '%s\n' "$GITHUB_SHA" > ci/v85-resource-fingerprint-drift-probe-telemetry/source-ref.txt
          printf '{"kind":"BUILD_SMOKE","schema_version":"cp-v85-resource-fingerprint-drift-probe-v1","scenario_id":"resource-fingerprint-component-drift-probe","control_sync":"DIAGNOSTIC_ONLY_ZERO_RESOURCE_EFFECT","audit":"SANITIZED_ADMISSION_FINAL_COMPONENT_COMPARISON"}\n' | curl -fsS -X POST "$ENDPOINT" -H 'Content-Type: application/json' --data-binary @- >/dev/null

      - name: End-to-end fingerprint diagnostic static audit
        shell: bash
        run: |
          set -euo pipefail
          ROOT=runtime_probes/chatgpt-webview-stable-probe
          PKG=$ROOT/app/src/main/java/com/homayounisaghar/chatgptwebviewprobe
          ACT=$PKG/OrchestratorResourceToolsV85Activity.java
          SVC=$PKG/ControlPlaneAccessibilityServiceV51.java
          MAN=$ROOT/app/src/main/AndroidManifest.xml
          GRADLE=$ROOT/app/build.gradle
          test -f "$ACT" && test -f "$SVC" && test -f "$PKG/TelemetryConfigV85.java"
          grep -Fq "applicationId 'com.homayounisaghar.chatgptwebviewprobe.diag'" "$GRADLE"
          grep -Fq 'versionCode 86' "$GRADLE"
          grep -Fq "versionName '0.83-stable-diag-resource-fingerprint-drift-probe'" "$GRADLE"
          grep -Fq 'OrchestratorResourceToolsV85Activity' "$MAN"
          grep -Fq 'ControlPlaneAccessibilityServiceV51' "$MAN"
          python3 - "$ACT" <<'PY'
          import pathlib,re,sys
          s=pathlib.Path(sys.argv[1]).read_text()
          for x in ['V64_NORMALIZED_FNV_CONTEXT','diagnostic_only','assistant_hash_equal','assistant_len_equal','preceding_user_hash_equal','preceding_user_len_equal','v64_tuple_all_equal','projection_tuple_all_equal','claim_assistant_ordinal','final_assistant_ordinal','PASS_RESOURCE_FINGERPRINT_RECONSTRUCTION_MATCH_DIAGNOSTIC_NO_ACTION','resourceV85DiagnosticOnly()','NONE_DIAGNOSTIC','raw_url_remote",false','raw_text_remote",false','raw_html_remote",false']:
              assert x in s,x
          fm=re.search(r'    private String assistantLocalResourceFinalV82Js\([^\n]*\)\{.*?\n    \}\n\n',s,re.S); assert fm
          assert '.click();' not in fm.group(0)
          assert s.index('if(resourceV85DiagnosticOnly())') < s.index('if(js){boolean clicked=')
          for bad in ['elementFromPoint','document.evaluate','XPathResult','getCookie(','CookieManager','addJavascriptInterface','Thread.sleep','ScheduledExecutorService','TimerTask']:
              assert bad not in s,bad
          print('PASS v0.83 workflow static audit: zero-effect diagnostic boundary and sanitized component comparison')
          PY

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - name: Build and prove exact artifact identity
        shell: bash
        run: |
          set -euo pipefail
          sdkmanager "platforms;android-35" "build-tools;35.0.0"
          echo "sdk.dir=$ANDROID_SDK_ROOT" > runtime_probes/chatgpt-webview-stable-probe/local.properties
          curl -fsSL https://services.gradle.org/distributions/gradle-8.10.2-bin.zip -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d /tmp
          cd runtime_probes/chatgpt-webview-stable-probe
          /tmp/gradle-8.10.2/bin/gradle --no-daemon :app:assembleRelease --stacktrace
          cd ../..
          OUT=ci/v85-resource-fingerprint-drift-probe-out
          mkdir -p "$OUT"
          UNSIGNED=runtime_probes/chatgpt-webview-stable-probe/app/build/outputs/apk/release/app-release-unsigned.apk
          ALIGNED="$OUT/chatgpt-webview-v0.83-resource-fingerprint-drift-probe-exact-source-aligned-unsigned.apk"
          BT="$ANDROID_SDK_ROOT/build-tools/35.0.0"
          "$BT/zipalign" -f -p 4 "$UNSIGNED" "$ALIGNED"
          "$BT/zipalign" -c -v 4 "$ALIGNED" > "$OUT/zipalign-verify.txt"
          "$BT/aapt" dump badging "$ALIGNED" > "$OUT/badging.txt"
          "$BT/aapt" dump permissions "$ALIGNED" > "$OUT/permissions.txt"
          grep -Fq "package: name='com.homayounisaghar.chatgptwebviewprobe.diag' versionCode='86' versionName='0.83-stable-diag-resource-fingerprint-drift-probe'" "$OUT/badging.txt"
          grep -Fq "launchable-activity: name='com.homayounisaghar.chatgptwebviewprobe.OrchestratorResourceToolsV85Activity'" "$OUT/badging.txt"
          test "$(grep -c '^uses-permission:' "$OUT/permissions.txt")" -eq 3
          grep -Fq 'android.permission.INTERNET' "$OUT/permissions.txt"
          grep -Fq 'android.permission.RECORD_AUDIO' "$OUT/permissions.txt"
          grep -Fq 'android.permission.MODIFY_AUDIO_SETTINGS' "$OUT/permissions.txt"
          cp "$BT/lib/apksigner.jar" "$OUT/apksigner.jar"
          java -jar "$OUT/apksigner.jar" --version > "$OUT/APKSIGNER_VERSION.txt"
          sha256sum "$OUT/apksigner.jar" > "$OUT/APKSIGNER_JAR_SHA256.txt"
          printf '%s\n' "$GITHUB_SHA" > "$OUT/SOURCE_REF.txt"
          printf '%s\n' 'RESOURCE_FINGERPRINT_COMPONENT_DRIFT_DIAGNOSTIC_ONLY' > "$OUT/FRONTIER_MODE.txt"
          printf '%s\n' 'V64_AUTHORITY_PLUS_NONAUTHORITATIVE_NFKC_PROJECTION_DIAGNOSTIC' > "$OUT/TURN_IDENTITY_POLICY.txt"
          printf '%s\n' 'ZERO_RESOURCE_EFFECT_DIAGNOSTIC' > "$OUT/CONTROL_SYNC.txt"
          printf '%s\n' 'NO_GEOMETRY_SELECTION_OR_ACTUATION_IN_DIAGNOSTIC' > "$OUT/GEOMETRY_POLICY.txt"
          printf '%s\n' 'b7aea08158252dcad79345608ce2e76e970e4cf613414db299579f46d9924c48' > "$OUT/EXPECTED_SIGNER_CERT_SHA256.txt"
          sha256sum "$ALIGNED" > "$OUT/ALIGNED_UNSIGNED_SHA256.txt"

      - uses: actions/upload-artifact@v4
        with:
          name: chatgpt-webview-v0.83-resource-fingerprint-drift-probe-${{ github.run_id }}-unsigned
          path: ci/v85-resource-fingerprint-drift-probe-out
          if-no-files-found: error
          retention-days: 30

      - uses: actions/upload-artifact@v4
        with:
          name: chatgpt-webview-v0.83-resource-fingerprint-drift-probe-${{ github.run_id }}-telemetry
          path: ci/v85-resource-fingerprint-drift-probe-telemetry
          if-no-files-found: error
          retention-days: 30
