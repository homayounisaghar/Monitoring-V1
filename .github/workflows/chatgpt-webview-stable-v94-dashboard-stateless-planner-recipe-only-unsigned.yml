name: ChatGPT WebView v0.90.0 Dashboard stateless Planner recipe-only unsigned

on:
  workflow_dispatch:
  push:
    branches:
      - project/chatgpt-webview-poc
    paths:
      - '.github/workflows/chatgpt-webview-stable-v94-dashboard-stateless-planner-recipe-only-unsigned.yml'
      - 'ci/generate_chatgpt_webview_v94_dashboard_stateless_planner_recipe_only.py'
      - 'ci/generate_chatgpt_webview_v93_dashboard_telemetry_budget_repair.py'
      - 'ci/generate_chatgpt_webview_v74_planner_format_repair.py'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      ROOT: runtime_probes/chatgpt-webview-stable-probe
      EXPECTED_PACKAGE: com.homayounisaghar.chatgptwebviewprobe.diag
      EXPECTED_VERSION: 0.90.0-stable-dashboard-stateless-planner-recipe-only
      EXPECTED_VERSION_CODE: '95'
      EXPECTED_LAUNCHER: com.homayounisaghar.chatgptwebviewprobe.OrchestratorDashboardV94PlannerIngressActivity
      EXPECTED_SIGNER_SHA256: b7aea08158252dcad79345608ce2e76e970e4cf613414db299579f46d9924c48
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Generate v0.90.0 recipe-only source
        shell: bash
        run: |
          set -euo pipefail
          python3 ci/generate_chatgpt_webview_v94_dashboard_stateless_planner_recipe_only.py

      - name: Create fresh telemetry collector and inject V94 config
        shell: bash
        run: |
          set -euo pipefail
          CFG="$ROOT/app/src/main/java/com/homayounisaghar/chatgptwebviewprobe/TelemetryConfigV94.java"
          TOKEN=$(curl -fsS -X POST https://webhook.site/token -H 'Accept: application/json' -H 'Content-Type: application/json' -d '{"default_status":200,"default_content":"OK","default_content_type":"text/plain"}' | python3 -c 'import json,sys;x=json.load(sys.stdin).get("uuid");assert x;print(x)')
          ENDPOINT="https://webhook.site/$TOKEN"
          READ="https://webhook.site/token/$TOKEN/requests?sorting=oldest&per_page=100"
          python3 - "$CFG" "$ENDPOINT" "$GITHUB_SHA" "$TOKEN" <<'PY'
          from pathlib import Path
          import sys
          p=Path(sys.argv[1]); s=p.read_text(); ep,sha,cid=sys.argv[2:]
          for old in ['CONFIGURED=false','COLLECTOR_ID=""','ENDPOINT=""','SOURCE_REF=""']: assert s.count(old)==1,old
          s=s.replace('CONFIGURED=false','CONFIGURED=true',1).replace('COLLECTOR_ID=""',f'COLLECTOR_ID="{cid}"',1).replace('ENDPOINT=""',f'ENDPOINT="{ep}"',1).replace('SOURCE_REF=""',f'SOURCE_REF="{sha}"',1)
          p.write_text(s)
          PY
          mkdir -p ci/v94-dashboard-telemetry
          printf '%s\n' "$TOKEN" > ci/v94-dashboard-telemetry/collector-id.txt
          printf '%s\n' "$READ" > ci/v94-dashboard-telemetry/read-url.txt
          printf '%s\n' "$GITHUB_SHA" > ci/v94-dashboard-telemetry/source-ref.txt
          printf '{"kind":"BUILD_SMOKE","schema_version":"cp-v94-dashboard-stateless-planner-recipe-only-v1","scenario_id":"dashboard-stateless-planner-recipe-only","source_ref":"%s","raw_text_remote":false,"raw_path_remote":false,"planner_chat_effect_dispatches":0,"target_chat_effect_dispatches":0}\n' "$GITHUB_SHA" | curl -fsS -X POST "$ENDPOINT" -H 'Content-Type: application/json' --data-binary @- >/dev/null

      - name: Static recipe-only safety audit
        shell: bash
        run: |
          set -euo pipefail
          SRC="$ROOT/app/src/main/java/com/homayounisaghar/chatgptwebviewprobe/OrchestratorDashboardV94PlannerIngressActivity.java"
          CFG="$ROOT/app/src/main/java/com/homayounisaghar/chatgptwebviewprobe/TelemetryConfigV94.java"
          GRADLE="$ROOT/app/build.gradle"
          MAN="$ROOT/app/src/main/AndroidManifest.xml"
          grep -Fq "applicationId 'com.homayounisaghar.chatgptwebviewprobe.diag'" "$GRADLE"
          grep -Eq 'versionCode[[:space:]]+95([^0-9]|$)' "$GRADLE"
          grep -Fq "versionName '0.90.0-stable-dashboard-stateless-planner-recipe-only'" "$GRADLE"
          grep -Fq 'OrchestratorDashboardV94PlannerIngressActivity' "$MAN"
          grep -Fq 'CONFIGURED=true' "$CFG"
          test "$(grep -c '<uses-permission' "$MAN")" -eq 3
          python3 - "$SRC" <<'PY'
          from pathlib import Path
          import sys
          s=Path(sys.argv[1]).read_text()
          for x in ['CF_PLANNER_REQUEST v1','cf-planner-recipe.v1','plannerReadJs','plannerSetDraftJs','plannerSendJs','dashboard_registry_v91.json','TURN_DOM_NOT_HYDRATED','STALE_FINISH_ROUTE_MISMATCH','MAX_READY_RETRIES=12','OBSERVE_WAIT_STARTED','target_chat_effect_dispatches']:
              assert x in s,x
          for bad in ['elementFromPoint','document.evaluate','XPathResult','dispatchTouchEvent','addJavascriptInterface','sendCurrentDraft']:
              assert bad not in s,bad
          assert s.count('.click()')==1
          assert s.count('worker.loadUrl(')==1
          assert 'targetChatEffectDispatches++' not in s and 'targetChatEffectDispatches +=' not in s
          assert 'raw_text_remote",false' in s and 'raw_path_remote",false' in s
          print('PASS v0.90.0 recipe-only static audit')
          PY

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3

      - name: Build deterministic unsigned release
        shell: bash
        run: |
          set -euo pipefail
          sdkmanager "platforms;android-35" "build-tools;35.0.0"
          echo "sdk.dir=$ANDROID_SDK_ROOT" > "$ROOT/local.properties"
          curl -fsSL https://services.gradle.org/distributions/gradle-8.10.2-bin.zip -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d /tmp
          cd "$ROOT"
          /tmp/gradle-8.10.2/bin/gradle --no-daemon :app:assembleRelease --stacktrace

      - name: Align verify and assemble Stage-A signing kit
        shell: bash
        run: |
          set -euo pipefail
          OUT=ci/v94-dashboard-stage-a
          mkdir -p "$OUT"
          UNSIGNED="$ROOT/app/build/outputs/apk/release/app-release-unsigned.apk"
          ALIGNED="$OUT/app-release-aligned-unsigned.apk"
          BT="$ANDROID_SDK_ROOT/build-tools/35.0.0"
          "$BT/zipalign" -f -p 4 "$UNSIGNED" "$ALIGNED"
          "$BT/zipalign" -c -v 4 "$ALIGNED" > "$OUT/zipalign-verify.txt"
          "$BT/aapt" dump badging "$ALIGNED" > "$OUT/badging.txt"
          "$BT/aapt" dump permissions "$ALIGNED" > "$OUT/permissions.txt"
          grep -Fq "package: name='$EXPECTED_PACKAGE' versionCode='$EXPECTED_VERSION_CODE' versionName='$EXPECTED_VERSION'" "$OUT/badging.txt"
          grep -Fq "launchable-activity: name='$EXPECTED_LAUNCHER'" "$OUT/badging.txt"
          test "$(grep -c '^uses-permission:' "$OUT/permissions.txt")" -eq 3
          sha256sum "$ALIGNED" | tee "$OUT/UNSIGNED_SHA256.txt"
          printf '%s\n' "$GITHUB_SHA" > "$OUT/SOURCE_REF.txt"
          printf '%s\n' "$EXPECTED_SIGNER_SHA256" > "$OUT/EXPECTED_SIGNER_CERT_SHA256.txt"
          cat > "$OUT/BUILD_METADATA.txt" <<EOF
          source_ref=$GITHUB_SHA
          package=$EXPECTED_PACKAGE
          version_code=$EXPECTED_VERSION_CODE
          version_name=$EXPECTED_VERSION
          launcher=$EXPECTED_LAUNCHER
          expected_signer_sha256=$EXPECTED_SIGNER_SHA256
          stage=unsigned_aligned_release
          behavior=dashboard_read_plane_preserved_stateless_planner_recipe_only_target_effects_disabled
          EOF
          cp "$SRC" "$OUT/"
          cp "$CFG" "$OUT/"
          cp "$MAN" "$OUT/"
          cp "$GRADLE" "$OUT/"

      - uses: actions/upload-artifact@v4
        with:
          name: chatgpt-webview-v0.90.0-dashboard-stateless-planner-recipe-only-${{ github.run_id }}-unsigned
          path: ci/v94-dashboard-stage-a
          if-no-files-found: error
          retention-days: 30
      - uses: actions/upload-artifact@v4
        with:
          name: chatgpt-webview-v0.90.0-dashboard-stateless-planner-recipe-only-${{ github.run_id }}-telemetry
          path: ci/v94-dashboard-telemetry
          if-no-files-found: error
          retention-days: 30
